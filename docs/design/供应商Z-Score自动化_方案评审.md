# 供应商 Z-Score 自动化系统 — 方案评审（v2.2 设计稿 + PRD v1.0）

> 评审对象：方案设计 v2.2 + 本次产出的 PRD v1.0
> 评审视角：可开发性、业务覆盖盲区、算法严谨性、过度设计、验收硬化
> 评审结论：**P0 三项已于 2026-08-04 经用户决议全部收口 → 开发启动就绪（go）**

---

## 一、总体评价

方案成熟度已显著高于一般「想法级」设计，核心资产是**6 个实测基准**——覆盖台湾/大陆/香港/卡塔尔四地、CAS/IFRS/在地三套准则、繁中/简中/英/双语、年报/季报两期，且用「年报 1.94 ↔ 季报年化 1.91」做了交叉验证。这已经是大多数自动化项目奢望的 ground truth。

但作为「可交付工程」，仍有几处必须在开发启动前收口，否则首版会出现「跑通 6 基准却跑不赢真实用户分布」的风险。

---

## 二、优势（应予保留）

1. **季报流量年化陷阱已捕获并实测**（不年化 1.428 vs 年化 1.910，差 0.48）——这是会导致风险区错判的真坑，方案第五·五部分处理到位。
2. **大陆 CAS 三规则有实证支撑**：留存收益两行拆分、利息费用嵌套子行、X4 用含少数股东口径，均来自鸣志真实数据，不是纸上谈兵。
3. **验证闸门 + 复式平衡**设计严谨，麦弗瑞（资不抵债/零收入）极端样本已覆盖，系统不会崩。
4. **三级映射成本可控**：L1/L2 覆盖绝大多数，L3 LLM 仅兜底，符合「小步快跑、不卖银弹」的工程观。
5. **6 基准双重回归断言**（年报+季报必须同时命中）是高质量的防回归设计。

---

## 三、P0 — 必须补强（开发前收口，否则首版失真）

### P0-1：行业→模型选择的「分类源」未决（直接决定 Z′/Z″ 选错）
- FR-7.1 与 PRD §14.2 开放问题 1 均指向同一盲区：**系统如何知道某供应商是制造业还是服务业？**
- 影响：选错模型（Z′ vs Z″）会直接改变系数与阈值，风险区可能整体偏移。鸣志是制造（已知），但自动化时若无标注，仅靠「营收/资产结构启发式」可靠性未验证。
- **建议**：开发前与用户敲定——默认**人工在 SupplierProfile 标注**（最稳），自动选择降级为「建议+低置信标记」而非「自动决策」。禁止在未知时静默猜模型。

> **【用户决议 2026-08-04】**：采用**人工确认行业类型**。行业由用户在 SupplierProfile 中标注，自动选择降级为「建议+低置信标记」，禁止静默猜模型。→ **P0-1 已收口**。

### P0-2：非上市单体样本仍是最大业务盲区
- 当前 6 基准中：瀚荃（上市）、鸣志（上市合并）、QSP/Microshield（IFRS 标准报表）。**零份大陆非上市单体 BS**。
- 而用户原话是「我会拿到不同供应商的**未上市**财报」——非上市恰是核心场景。非上市单体 BS 通常：无合并/母公司区分、科目更简陋、可能无现金流量表（无法做 G4 交叉）。
- **建议**：Phase 1 启动前至少补 1 份大陆非上市单体财报。否则「6 基准验收」不能代表真实用户分布，Phase 1 验收含金量打折。

> **【用户决议 2026-08-04】**：**暂时跳过非上市供应商财报**。接受「6 基准全为上市/标准 IFRS」现状，非上市单体作为已知盲区后置。→ **P0-2 已收口（接受风险）**。开发期将「非上市单体可能无合并表 / 科目简陋 / 无现金流表」作为容错设计目标，但不阻塞启动；Phase 1 验收仍按 6 基准执行。

### P0-3：币种可比性与阈值声明必须显式化
- 计算层面无问题：X1–X5 全是比率，币种自动抵消，Z 值跨币种可比（这是 Z-Score 优点，应写进 PRD 作为特性）。
- 但有两处必须显式声明：
  1. **输出层**：每个供应商必须标注原始币种；跨供应商「风险排序」只能基于 Z 值，**绝不能基于绝对金额列**（QAR 与 CNY 不可比）。PRD §8.1 有 currency 字段，但缺「禁止跨币种比金额」的硬规则。
  2. **阈值适用性**：Altman 原阈值基于美国上市/制造业样本，套到大陆非上市民企有统计偏差。输出必须强制标注「阈值基于 Altman 美国样本，对中国民企仅供参考」，并提供 threshold_override 默认配置位（PRD 已列为风险，但应升级为**输出强制声明**）。

> **【用户决议 2026-08-04】**：**暂时忽略**币种可比性声明与阈值适用性声明。Z 值跨币种可比的计算特性保留，但「标注原始币种 / 禁止跨币种比金额 / 阈值适用声明」暂不实现。→ **P0-3 已收口（暂缓）**。后续如接入真实多币种供应商分布再补此声明层。

---

## 四、P1 — 应优化（开发期处理，不阻断启动）

1. **年化季节性偏差未量化**：×4 假设均匀分布。消费电子等强季节性供应商，季报年化会系统性偏差。建议：①报告标注「年化基于均匀假设」；②可选支持「同比去年同季×4」更稳。当前仅实践注意项，应升为可配置策略。
2. **LLM 兜底未定价/未 PoC**：L3 是成本与延迟主变量，但无 token 预算、模型选型、失败降级。建议 Phase 2 前做 1 次 L3 PoC（拿未命中样本测命中率+成本）。
3. **置信度阈值未定义**：「低置信标记触发人工复核」的阈值（如 confidence<0.8）未硬化，FR-4.4 需补具体数。
4. **表格跨页/嵌套鲁棒性未测**：pdfplumber extract_tables 对合并单元格、跨页表头重复、附注插入易失败。6 基准里鸣志是规范表，真实供应商常有跨页结构。需补鲁棒性测试集。
5. **多期提取仅取最新期**：FR-3.4 只取新期，趋势靠跨年文件。可接受，但应明确写进 PRD，避免用户误以为单文件能出多年趋势。

---

## 五、P2 — 可商榷 / 潜在过度设计

1. **Streamlit UI 是否首版必需？** 内部工具，MVP 可先 CLI + Excel/HTML 输出，UI 后置，降低首版复杂度。
2. **SQLite vs 纯 JSON**：当前「JSON + SQLite」略冗余，MVP 纯 JSON + 断点文件足够，SQLite 可 Phase 3 引入。
3. **行业自动选择做成「启发式」可能过度**：更稳是默认人工标注（FR-8.2 已是），自动选择降为「建议」。避免错误自动选型带来误判。
4. **M4 模块职责略重**：映射+EBIT 塞在一起，EBIT 决策树可独立为 M4.5。非阻断，仅可读性。

---

## 六、验收标准硬化建议

| 当前表述 | 问题 | 建议硬化 |
|---------|------|---------|
| 「误差 < 1%」 | 未说是 Z 值还是逐因子 | Z 值相对误差 < 1% **且** 各 X 因子相对误差 < 2%（X4 因小分母可放宽至 < 5%） |
| 风险区一致 | 未说边界容忍 | 必须 6/6 完全一致，灰色↔安全边界不允许摇摆 |
| 缺跨币种断言 | 币种抵消未验证 | 加单测：同一报表换币种数值，Z 值不变 |
| 麦弗瑞「自动修复」 | 未量化 | 明确 Z″ 误差 < 1% 且 EQ<0 时 X4 符号正确 |

---

## 七、结论与行动项

**结论**：方案已到「可开发」级别，6 基准是强资产。**2026-08-04 用户已对三项 P0 全部决议收口（人工确认行业 / 跳过非上市样本 / 暂缓币种阈值声明）→ 状态升级为「开发启动就绪（go）」**。

**收口后行动序列**：
1. ✅ ~~与用户敲定行业分类策略~~ → **已决议：人工标注优先，自动选择降为建议**。
2. ⏸️ ~~补非上市单体财报~~ → **已决议：暂时跳过，作为已知盲区后置**（容错设计保留）。
3. ⏸️ ~~PRD 补币种/阈值声明~~ → **已决议：暂时忽略**，保留 Z 值跨币种可比的计算特性即可。
4. **▶️ Phase 1 直接开干（M1–M6 + Excel 输出），UI/SQLite 后置减重**（见 P2 减重项）。
5. 年化策略、L3 PoC、置信度阈值在 Phase 2 前补齐（P1 项，不阻断启动）。

> 一句话：方向对、基准硬、坑抓得准；P0 三个决策未决项已按用户意志收口——**可以放心开干了**。非上市盲区与币种声明作为 P1/P2 期可补的已知项，不阻塞首版交付。
