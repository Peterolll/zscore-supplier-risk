# 供应商 Z-Score 自动化 · 二次校验层端到端验证报告

> 日期：2026-08-27｜范围：`lib/validation.ts` 的"位数/量级二次校验层"在**真实环境**下的表现
> 测试对象：paratera GLM-4V-Flash（即 8/27 已知会"双丢末位"的弱视觉模型）
> 测试样本：瀚荃 114Q4 合併財報（真实财报 PDF，繁体、金额单位千元）

---

## 一、验证环境与方式

| 项 | 内容 |
|---|---|
| AI 配置 | provider=custom，base_url=llmapi.paratera.com，vision_model=GLM-4V-Flash，key 已配置 |
| 调用路径 | 路由真实默认 `callMultimodalSplitModel`（裁切 BS/IS + 聚焦 prompt，上下文更小、准确率 8/8） |
| 校验层 | `applySanityChecks(suggestions, extractedFields)`，库内已确认值为可信锚点 |
| 对照 | `pdftotext` 抽取 PDF 原文，逐科目与 AI 返回值对账 |

---

## 二、核心结论

### ✅ 二次校验层有效（针对"漏末位/内部不自洽"）
真实调用中模拟"资产总计漏末位（÷10）"：因 `current_assets` 作为可信锚点远大于篡改值，校验层**同时**触发"资产负债表不平衡"与"资产总计小于流动资产"两条告警，confidence 降至 0.25。跨字段复校在真实数据上成立。

### ⚠️ 暴露更严重失效模式：「自洽但全错」
真实 AI 返回值与 PDF 原文**严重不符**：

| 科目 | AI 返回 | PDF 原文(千元) | 真实值(元) | 偏差 |
|---|---|---|---|---|
| 资产总计 | 126,604,932.01 | 5,920,713 | **5,920,713,000** | ≈1/47 |
| 流动资产 | 126,604,932.01 | 4,620,715 | 4,620,715,000 | ≈1/36 |
| 负债合计 | 51,000,000 | 2,120,477 | 2,120,477,000 | ≈1/42 |
| 权益总计 | 75,604,932.01 | 3,800,236 | 3,800,236,000 | ≈1/50 |
| 营业收入 | 147,087,809.03 | 3,355,450 | 3,355,450,000 | ≈1/23 |

模型读到的数字比原文**小约 40 倍**，且 `资产总计 == 流动资产`、`负债合计 == 流动负债`（到分完全一致）——典型"读错区块 / 未识别非流动科目，却拼出一张自洽假表"。

**关键问题**：该假表内部自洽（126.6M = 51M + 75.6M），导致
- 8/27 的 `value≠evidence` 交叉核验 → **未触发**（模型 value 与自身 evidence 一致）
- 本次新增的跨字段恒等式校验 → **未触发**（恒等式成立）

即：**内部一致性校验无法捕获"模型读错区块但产出平衡表"**。这比"漏末位"危害更大，因为结果看起来完全正常。

### ✅ 针对性加固：新增「总量与分量完全相同」高信号校验
在恒等式之外，新增 ①-b 规则：当 `资产总计≈流动资产（到分一致）` / `负债合计≈流动负债` / `权益总计≈留存收益` 时直接告警"疑似模型未识别非流动科目或读取错误区块，请核对原文"。

验证：
- **A（真实折叠输出）**：5 个相关字段全部被新规则捕获 ✅
- **B（真实原文数字 5.9B≠4.6B 且精确平衡）**：**零误报** ✅
- **C（漏末位模拟）**：跨字段捕获不变 ✅

---

## 三、仍存在的根本限制（需更强模型）

当"流水线也漏提该字段 **且** 模型把量级整体读错（非折叠、非漏末位）"时，系统**无任何可信锚点**，内部一致性校验无解。治本仍依赖：
1. 在 `/settings` 换更强视觉模型（GLM-4.5V / GPT-4o）；
2. 或采用"双模型交叉 / 自一致性多采样"作为第二意见；
3. 或把 Python 流水线提取值（若可得）作为 AI 建议的对比锚点，量级差 >N 倍即告警。

---

## 四、交付物与状态

- 新增 `lib/validation.ts`：`applySanityChecks`（①恒等式 ②①-b 完全相同 ③分量≤总量 ④利润≤营收 ⑤量级下限 ⑥总额非负）
- 接入 `app/api/runs/[runId]/ai-analyze/route.ts`：EBIT 推算后调用
- `tsc --noEmit` 零错误；8 场景单测 + 真实链路 + 原文对账全部通过
- 前端 `EditableRunDetail.tsx:461` 的 `⚠ {warning}` 渲染已自动可见，无需改 UI
